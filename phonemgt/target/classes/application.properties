server.port=8586


### Spring properties
# Service registers under this name
spring.application.name= computermgt

# Discovery Server Access
eureka.client.serviceUrl.defaultZone= http://localhost:8484/eureka/

spring.mvc.view.prefix = /WEB-INF/views/
spring.mvc.view.suffix = .jsp


# ===============================
# = DATA SOURCE
# ===============================
spring.datasource.driver-class-name=org.hsqldb.jdbcDriver
spring.datasource.url = jdbc:hsqldb:mem:employeesdb
spring.datasource.username = dharma
spring.datasource.password=*Dharma6

# ===============================
# JPA / HIBERNATE / with hsql
# ===============================
spring.jpa.show-sql = false
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.HSQLDialect
hibernate.format_sql=true;
# ===============================


